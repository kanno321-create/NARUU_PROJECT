[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "cd src && python -m uvicorn kis_estimator_core.api.main:app --host 0.0.0.0 --port ${PORT:-8000}"
